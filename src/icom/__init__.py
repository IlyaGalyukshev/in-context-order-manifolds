"""icom — in-context order manifolds.

Pipeline: generator → battery (behavior) → extraction (activations) → geometry → stats.
Each step is deterministic given (config, seed) and idempotent, keyed by content hash.
"""

__version__ = "0.1.0"
